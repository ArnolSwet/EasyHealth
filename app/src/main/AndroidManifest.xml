<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easyhealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".prova"
            android:label="@string/title_activity_prova"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".SettingsTrainer" />
        <activity android:name=".ClientForTrainer" />
        <activity android:name=".PersonalDiet" />
        <activity android:name=".YourClasses" />
        <activity android:name=".Chat" />
        <activity android:name=".AddFood" />
        <activity android:name=".MakeADiet" />
        <activity android:name=".Statistics" />
        <activity android:name=".activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>